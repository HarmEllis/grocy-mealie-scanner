.co-develop/
.claude/
.temp/
node_modules/
dist/
.env
*.log

# Firmware signing keys. Keep private keys out of git; store the release key
# in a secret manager and in the ESP_SECURE_BOOT_SIGNING_KEY_B64 GitHub secret.
*.pem
*.key
firmware/secure_boot_signing_key.pem

# ESP-IDF build artifacts
firmware/build/
firmware/build_*/
firmware/managed_components/
firmware/sdkconfig
firmware/sdkconfig.old
firmware/.idf_build_apps.json

# devcontainer
.devcontainer/.env
bins/
flash-server/bins/
